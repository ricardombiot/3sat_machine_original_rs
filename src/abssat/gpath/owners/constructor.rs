

use crate::abssat::gpath::owners::Owners49;

impl Owners49 {
    pub fn new() -> Self { 
        let table: Vec<i64> = Vec::new();
        let total_step: i32 = 0;
        let is_valid = true;
        Self { table , total_step ,is_valid} 
    }
}